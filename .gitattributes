# 行尾规范：仓库与全部检出统一 LF。
#
# 为什么：开发环境 Windows 与部署环境 Docker(Linux) 的消费者完全一致——
# Go 工具链（gofmt/goimports）原生写 LF，容器内 shell/entrypoint 遇 CRLF
# 会以 /bin/sh^M 崩溃，镜像层 diff 也要求确定性行尾。eol=lf 使 Windows
# 检出不转 CRLF，从根上消除 autocrlf 拉锯（本机配套 git config
# core.autocrlf false 或 input；仓库内 .gitattributes 永远是权威）。
#
# 例外约定：将来新增必须 CRLF 的 Windows 专属脚本（*.bat/*.cmd/*.ps1）
# 时，在此显式加 `*.bat text eol=crlf` 一类条目；二进制资产依赖
# text=auto 的自动检测，无需逐一登记。
* text=auto eol=lf
