server:
  http:
    addr: ":8080"

transport:
  websocket:
    addr: ":9080"
    path: "/ws"
  grpc:
    addr: ":9090"

# Proxy server configuration (implements ProxyService for backend integrations)
proxy:
  addr: ":9001"
  insecure: true